    {% extends 'common/app.html.twig' %}

    {% block header %}
    <h1>Category edit</h1>
    {% endblock %}
    
    {% block content %}

    {{ form(edit_form) }}

        <div class="record-actions">
    <a class="button" href="{{ path('category') }}">
        Back to the list
    </a>
                <div style="float:left">
        {{ form(delete_form) }}
        </div>
        <div style="clear:both"></div>
    </div>
    {% endblock %}
