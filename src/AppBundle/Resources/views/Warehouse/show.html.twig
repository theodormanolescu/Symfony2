    {% extends 'common/app.html.twig' %}

    {% block header%}
    <h1>Warehouse</h1>
    {% endblock %}

    {% block content%}

    <table class="record_properties">
        <tbody>            <tr>
                <th>Id</th>                <td>
                    {{ entity.id }}
                </td>            </tr>            <tr>
                <th>Name</th>                <td>
                    {{ entity.name }}
                </td>            </tr>            <tr>
                <th>Address</th>                <td>
                    {{ entity.address }}
                </td>            </tr>        </tbody>
    </table>

        <div class="record-actions">
    <a class="button" href="{{ path('warehouse') }}">
        Back to the list
    </a>
            <a class="button" href="{{ path('warehouse_edit', 
            { 'id': entity.id }) }}">
            Edit
        </a>
                <div style="float:left">
        {{ form(delete_form) }}
        </div>
        <div style="clear:both"></div>
    </div>
    {% endblock %}
