    {% extends 'common/app.html.twig' %}
    {% block header%}<h1>Warehouse list</h1>{% endblock %}
    {% block content%}
    <table class="records_list">
        <thead>
        <tr>                <th>Id</th>                <th>Name</th>                <th>Address</th>            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
        <tr>                    <td>
                        <a href="{{ path('warehouse_show', { 'id': entity.id }) }}">
                            {{ entity.id }}</a>
                    </td>                    <td>{{ entity.name }}</td>                    <td>{{ entity.address }}</td>                    <td>
                <ul>
                    <li>
                        <a href="{{ path('warehouse_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('warehouse_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                    </td>        </tr>
        {% endfor %}
        </tbody>
    </table>
            <a class="button" href="{{ path('warehouse_new') }}">
            New Warehouse
        </a>
        {% endblock %}
