<div class="actions">
    {%- for action in record_actions %}
        <a class="button" href="{{ "{{ path('" ~ route_name_prefix ~ "_" ~
        action ~ "', { 'id': entity."~ identifier ~" }) }}" }}">
            {{ action }}
        </a>
    {%- endfor %}
</div>